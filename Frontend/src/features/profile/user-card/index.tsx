import { UserAvatar } from '../../../entities/user/ui/user-avatar';

export const ProfileUserCard = () => {
    return (
        <div>
            <UserAvatar />
            <div>profile info</div>
        </div>
    );
};
